# Example vault

Minimal Obsidian vault used as the docker-compose default when `OAS_VAULT_HOST_PATH` is unset. Do not commit churn from opening this vault in Obsidian — `workspace.json` and `app.json` rewrite themselves with new leaf IDs and per-machine state.
