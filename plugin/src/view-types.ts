/** Obsidian view-type identifiers. Lives in its own file so modules that
 * only need the constant don't pull in the xterm-laden TerminalView. */

export const VIEW_TYPE_TERMINAL = "agent-sandbox-terminal-view";
